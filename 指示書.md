# キャラクターBot モック構築指示書

## ゴール

Streamlit + Gemini API で、原作テキストをベースにしたキャラクターBotのモックを作る。
Streamlit Cloud にデプロイし、クライアントにURLを共有できる状態にする。

## 技術スタック

- Python / Streamlit
- Google Generative AI SDK (`google-generativeai`)
- Gemini 2.0 Flash（無料枠）
- Streamlit Cloud（デプロイ先）

## ディレクトリ構成

```
character-bot-demo/
├── app.py              # メインアプリ（1ファイル完結）
├── requirements.txt    # streamlit, google-generativeai
└── .streamlit/
    └── secrets.toml    # ローカル実行用（.gitignore対象）
```

※ `.streamlit/secrets.toml` はGitHubに push しないこと。

## app.py の要件

### キャラクター設定

ファイル冒頭に以下の定数を定義する。デプロイ後に中身を差し替えるだけで別キャラにも対応できるようにする。

- `SOURCE_TEXT`: 原作テキスト全文を文字列で格納（数千文字程度を想定）
- `CHARACTER_NAME`: キャラクター名
- `CHARACTER_SETTING`: システムプロンプト。以下のルールを含むこと
  - 原作の性格・口調・価値観を忠実に再現する
  - 原作に書かれていない事実を勝手に創作しない
  - 現代の話題にはキャラクターの視点で自然に応答する
  - 一人称や語尾などキャラ特有の話し方を維持する

### Gemini API 連携

- `google-generativeai` SDK を使用
- APIキーは `st.secrets["GEMINI_API_KEY"]` から取得
- モデル: `gemini-2.0-flash`
- `system_instruction` に `CHARACTER_SETTING` を渡す
- `model.start_chat()` でチャットセッションを管理し、会話履歴を保持する

### UI

- `st.set_page_config` でページタイトルとアイコンを設定
- `st.title` と `st.caption` でヘッダー表示
- `st.chat_message` / `st.chat_input` でチャットUI
- `st.session_state` でメッセージ履歴とチャットセッションを管理
- ストリーミング応答: `send_message(prompt, stream=True)` でチャンクを受け取り、`st.empty()` + `placeholder.markdown()` で逐次表示する。入力中は末尾に `▌` を表示。

### ダミーデータ

初期状態では「吾輩は猫である」の冒頭をダミーとして `SOURCE_TEXT` に入れておく。動作確認後に本番テキストへ差し替える。

## requirements.txt

```
streamlit
google-generativeai
```

## .gitignore

```
.streamlit/secrets.toml
```

## ローカル実行手順

```bash
pip install -r requirements.txt
mkdir -p .streamlit
echo 'GEMINI_API_KEY = "YOUR_KEY_HERE"' > .streamlit/secrets.toml
streamlit run app.py
```

## Streamlit Cloud デプロイ手順

1. GitHubにパブリックリポジトリとして push（`app.py` と `requirements.txt` のみ）
2. https://share.streamlit.io でリポジトリを選択しデプロイ
3. Settings → Secrets に `GEMINI_API_KEY = "実際のキー"` を設定
4. `https://your-app-name.streamlit.app` をクライアントに共有

## 注意事項

- `.streamlit/secrets.toml` は絶対に push しない
- パブリックリポジトリなのでソースコードは公開される（APIキーはsecretsで保護済み）
- 本番リリース時は Cloudflare Workers + Pages 構成に移行予定
