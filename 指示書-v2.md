# キャラクターBot 構築指示書 v2（Next.js 版 / 現行）

> **本書の位置づけ**: `指示書.md`（v1）は initial spec（Streamlit + Python 想定）。本書は **2026-03 に Next.js + Vercel AI SDK へ移行した後の現行構成**を反映する。新規開発・運用は本書を参照すること。v1 は史料として保持。

---

## ゴール

原作テキストをベースにしたキャラクターBotを Next.js + Vercel で運用する。クライアントに URL を共有できる状態で、無料〜低コストの範囲で動かせること。

---

## 技術スタック

| レイヤー | 採用技術 |
|---------|---------|
| フレームワーク | Next.js 15（App Router） |
| 言語 | TypeScript 5 |
| UI | React 19 + TailwindCSS 4 |
| LLM SDK | [Vercel AI SDK](https://sdk.vercel.ai/)（`ai` + `@ai-sdk/google`） |
| LLM モデル | Google Gemini 2.5 Pro（無料枠あり） |
| ホスティング | Vercel |
| 認証情報 | 環境変数（`GOOGLE_GENERATIVE_AI_API_KEY`） |

---

## ディレクトリ構成

```
character-bot-demo/
├── app/
│   ├── page.tsx              # チャットUI（useChat フック）
│   ├── layout.tsx            # ルートレイアウト
│   ├── globals.css           # Tailwind 含むグローバルCSS
│   └── api/
│       └── chat/
│           └── route.ts      # POST /api/chat — LLM 呼び出し
├── lib/
│   └── character.ts          # CHARACTER_NAME / SOURCE_TEXT / CHARACTER_SETTING を export
├── docs/                     # 運用ガイド・クライアントQA
├── 指示書.md                  # v1 史料（変更禁止）
├── 指示書-v2.md               # 本書（現行）
└── package.json
```

---

## キャラクター設定の管理

`lib/character.ts` に以下を定義し、デプロイ後の差し替えで別キャラにも対応可:

- **`CHARACTER_NAME`**: キャラクター名（現状: `"チャ・レノン"`）
- **`DISPLAY_NAME`**: 表示名（現状: `"お茶好きの兄ちゃん"`）
- **`SOURCE_TEXT`**: 原作テキスト全文（数千文字想定）
- **`CHARACTER_SETTING`**: システムプロンプト。下記を必ず含めること
  - **絶対遵守事項（S1-S4）**: プロンプトインジェクション対策 / 他社商品名禁止 / 薬機法回避 / 個人情報の扱い
  - **キャラクター設定**: 役割・口調・性格・スタンス
  - **応答ルール**: 1回 3〜5文 / 知識ベース外の創作禁止 等

---

## API ルート

### `app/api/chat/route.ts`

```ts
import { streamText } from "ai";
import { google } from "@ai-sdk/google";
import { CHARACTER_SETTING } from "@/lib/character";

export async function POST(req: Request) {
  const { messages } = await req.json();
  const result = streamText({
    model: google("gemini-2.5-pro"),
    system: CHARACTER_SETTING,
    messages,
  });
  return result.toDataStreamResponse();
}
```

- `streamText` で逐次レスポンスをストリーミング
- `system` に `CHARACTER_SETTING` を渡し、すべてのターンで適用

---

## デプロイ手順（Vercel）

1. GitHub に push（メインブランチ）
2. Vercel ダッシュボードで GitHub リポジトリを Import
3. Environment Variables に `GOOGLE_GENERATIVE_AI_API_KEY` を設定
4. Deploy → 発行された URL をクライアントに共有

`vercel.json` 等の追加設定は不要（Next.js 標準で動作）。

---

## ローカル実行

```bash
npm install
echo "GOOGLE_GENERATIVE_AI_API_KEY=xxxx" > .env.local
npm run dev
# http://localhost:3000 でアクセス
```

---

## ランニングコスト見積（参考）

| 項目 | 費用 | 備考 |
|------|------|------|
| Vercel Hobby | ¥0 | 個人・PoC 利用可。商用は要 Pro 検討 |
| GitHub | ¥0 | パブリック / プライベートとも無料 |
| Gemini 2.5 Pro API | 従量 | 無料枠あり。会話量に応じて変動 |

`docs/operation-guide.md` は v1（Streamlit）時代の記述あり。Next.js 移行後の最新コストは別途見直しが必要。

---

## v1（指示書.md）からの主な変更点

| 観点 | v1（Streamlit） | v2（Next.js / 現行） |
|------|-----------------|---------------------|
| 言語 | Python | TypeScript |
| 単一ファイル | `app.py` 完結 | `app/` + `lib/` + `app/api/` に分離 |
| 認証情報 | `.streamlit/secrets.toml` | Vercel 環境変数 / `.env.local` |
| LLM SDK | `google-generativeai` | `@ai-sdk/google` (Vercel AI SDK) |
| ストリーミング | Streamlit の `write_stream` | `streamText` + `toDataStreamResponse` |

絶対遵守事項（S1-S4）と キャラクター設定本文は v1 から変更なく踏襲。
